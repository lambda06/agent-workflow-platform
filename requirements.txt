fastapi==0.135.3          # Web server framework for the API
uvicorn[standard]==0.44.0  # ASGI web server to run the FastAPI app
langgraph==1.1.6           # Agent orchestration framework
langchain-google-genai==4.2.1  # Integration for Gemini LLM
langchain-core==1.2.28     # Core abstractions for agent logic
pydantic==2.12.5           # v2 data validation and serialization
pydantic-settings==2.13.1  # Configuration management from environment variables
# PostgreSQL — psycopg3 (psycopg[binary]) replaces asyncpg across the whole stack.
# It is the same driver used internally by LangGraph's AsyncPostgresSaver, so agents
# and the LangGraph checkpointer now share one driver with zero protocol conflicts.
# asyncpg was removed: it uses a different wire-protocol implementation from psycopg3
# and caused SSL negotiation failures when both ran against the same Supabase instance.
# sqlalchemy[asyncio] was removed: it was never used — raw psycopg3 is sufficient for
# the lean JSONB inserts this project requires.
psycopg[binary]==3.2.9     # psycopg3 async PostgreSQL driver (binary wheel for speed)
psycopg-pool==3.2.8        # AsyncConnectionPool — shared pool used by all agents
upstash-redis==1.7.0       # Client for Upstash Redis caching
langfuse==4.2.0            # Observability and tracing for LLM applications
python-dotenv==1.2.2       # Local development support for loading .env files
httpx==0.28.1              # Async HTTP client used by core integrations
slack_sdk==3.34.0          # Slack Web API client (AsyncWebClient for bot verification)
aiohttp==3.11.18           # Required async transport for slack_sdk's AsyncWebClient
google-api-python-client==2.169.0  # Gmail REST API client (email_tools.py + verify_connections.py)
google-auth-httplib2==0.2.0        # httplib2 transport for google-auth
google-auth-oauthlib==1.2.2        # OAuth2 flow helpers for Google APIs
